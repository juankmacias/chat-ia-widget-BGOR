# API Key de Anthropic (consíguela en https://console.anthropic.com)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxx

# Connection string de Neon (CREA UN PROYECTO NUEVO EN NEON para BGOR, no reutilices el de PowerMix)
DATABASE_URL=postgresql://usuario:password@ep-xxx.neon.tech/dbname?sslmode=require

# Escape de emergencia: pon =true SÓLO si la conexión TLS a tu base falla por
# certificado no validable. Por defecto (sin esta variable) se valida el cert.
# DATABASE_SSL_NO_VERIFY=true

# Puerto del servidor (opcional)
PORT=3000

# Dominio permitido para CORS (en producción, pon el del sitio de BGOR)
ALLOWED_ORIGIN=https://bgor.com.co

# Credenciales para la página admin (/admin)
ADMIN_USER=admin
ADMIN_PASS=cámbiame
